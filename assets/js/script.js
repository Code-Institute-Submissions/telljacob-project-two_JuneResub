// Import input data
const rounds = document.querySelector(".input-rounds");
const work = document.querySelector(".input-work");
const rest = document.querySelector(".input-rest");

// Import start, stop and reset buttons
var start = document.querySelector(".start-workout");
var stop = document.querySelector(".stop-workout");
var reset = document.querySelector(".reset-workout");

function setTimer(work) {
    work = 
};

function setRound(rounds) {
    rounds = 
};